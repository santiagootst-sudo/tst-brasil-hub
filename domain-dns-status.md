# Status de configuração DNS

A administração autenticada do `tstbrasilhub.com.br` está acessível no Registro.br. O domínio está publicado, com expiração em 14/08/2027, e utiliza atualmente os servidores DNS do próprio Registro.br.

A opção `Configurar zona DNS` foi aberta para consultar/adicionar os registros. Nenhuma alteração foi salva até este ponto.

Destino de hospedagem confirmado: `https://tstportal-lhmdcupa.manus.space`.
