import { ArrowRight, BookOpen, BriefcaseBusiness, Building2, CircleHelp, FileBadge2, Loader2, Plus, ShieldCheck, UserRoundCheck } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { toast } from "sonner";
import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";

type WorkspaceKind = "autonomo" | "clt";
const shared = [{ icon: BookOpen, title: "Biblioteca", text: "Normas, NRs e materiais técnicos" }, { icon: FileBadge2, title: "Materiais", text: "Modelos e checklists para SST" }, { icon: CircleHelp, title: "Suporte", text: "Acompanhe seus chamados" }];

export default function WorkspaceHub() {
  const [, setLocation] = useLocation();
  const { user, loading } = useAuth({ redirectOnUnauthenticated: true });
  const utils = trpc.useUtils();
  const { data: workspaces, isLoading } = trpc.portal.workspaces.useQuery(undefined, { enabled: Boolean(user) });
  const billing = trpc.billing.status.useQuery(undefined, { enabled: Boolean(user) });
  const [form, setForm] = useState<{ kind: WorkspaceKind; name: string } | null>(null);
  const createWorkspace = trpc.portal.createWorkspace.useMutation({
    onSuccess: workspace => { utils.portal.workspaces.invalidate(); setForm(null); setLocation(`/app/pgr?workspace=${workspace.id}`); toast.success("Ambiente criado. Agora você já pode iniciar o PGR."); },
    onError: error => toast.error(error.message),
  });
  const manageSubscription = trpc.billing.manage.useMutation({
    onSuccess: ({ url }) => window.open(url, "_blank", "noopener"),
    onError: error => toast.error(error.message),
  });
  if (loading || isLoading) return <div className="grid min-h-screen place-items-center"><Loader2 className="animate-spin text-[#0c7474]" /></div>;
  const grouped = { autonomo: workspaces?.filter(w => w.kind === "autonomo") ?? [], clt: workspaces?.filter(w => w.kind === "clt") ?? [] };
  const open = (id: number) => setLocation(`/app/pgr?workspace=${id}`);

  const billingSuccess = new URLSearchParams(window.location.search).get("billing") === "success";
  return <DashboardLayout title="Meus ambientes"><div className="mx-auto max-w-6xl">{billingSuccess && <div className="mb-5 rounded-2xl border border-[#b9e3d7] bg-[#f1fcf7] px-5 py-4 text-sm text-[#17664f]">Recebemos a confirmação do checkout. A assinatura será liberada assim que o pagamento for processado.</div>}<div className="flex flex-col justify-between gap-4 md:flex-row md:items-start"><p className="text-sm text-[#668087]">Olá, {user?.name?.split(" ")[0] || "profissional"}. Você pode acessar seus ambientes ou usar as ferramentas compartilhadas.</p>{billing.data?.subscription && <Button onClick={() => manageSubscription.mutate()} variant="outline" className="rounded-xl border-[#bddbd5] text-[#0c7474]">Gerenciar assinatura</Button>}</div><div className="mt-9 flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-[#e8f6f1] text-[#0c7474]"><Building2 className="h-6 w-6" /></span><div><h2 className="text-2xl font-bold">Escolha seu ambiente de trabalho</h2><p className="text-sm text-[#698187]">TST Autônomo e TST CLT são contextos separados, com dados organizados por perfil e empresa.</p></div></div><section className="mt-7 grid gap-6 lg:grid-cols-2">{(["autonomo", "clt"] as WorkspaceKind[]).map(kind => { const autonomous = kind === "autonomo"; const entries = grouped[kind]; const Icon = autonomous ? BriefcaseBusiness : UserRoundCheck; return <article key={kind} className={`overflow-hidden rounded-[1.8rem] border bg-white ${autonomous ? "border-[#a6ddcf]" : "border-[#c5dff3]"}`}><div className={`flex items-start justify-between p-7 ${autonomous ? "bg-[#f4fcf8]" : "bg-[#f4f9ff]"}`}><div><span className={`rounded-lg px-3 py-1 text-[10px] font-bold uppercase tracking-[.12em] ${autonomous ? "bg-[#d9f1e7] text-[#168c89]" : "bg-[#dbeeff] text-[#2165a9]"}`}>TST {autonomous ? "Autônomo" : "CLT"}</span><h3 className="mt-5 text-2xl font-bold">{autonomous ? "Atue como prestador de serviços" : "Atue como colaborador da empresa"}</h3><p className="mt-3 max-w-sm text-sm leading-6 text-[#5d7479]">{autonomous ? "Gerencie clientes, empresas, documentos e PGRs de forma independente." : "Acompanhe o PGR, indicadores e demandas da organização onde você trabalha."}</p></div><span className={`grid h-12 w-12 place-items-center rounded-2xl ${autonomous ? "bg-[#d9f1e7] text-[#0c8c89]" : "bg-[#dbeeff] text-[#2165a9]"}`}><Icon className="h-6 w-6" /></span></div><div className="min-h-42 p-6">{entries.length ? <div className="space-y-3">{entries.map(workspace => <button key={workspace.id} onClick={() => open(workspace.id)} className="flex w-full items-center justify-between rounded-xl border border-[#deece9] px-4 py-3 text-left hover:border-[#0c8c89] hover:bg-[#f7fbfa]"><span><strong className="block text-sm">{workspace.name}</strong><small className="text-xs text-[#6f858a]">Perfil {workspace.role === "owner" ? "proprietário" : workspace.role}</small></span><ArrowRight className="h-4 w-4 text-[#0c7474]" /></button>)}</div> : <div className="rounded-xl border border-dashed border-[#c7ddd8] bg-[#fbfefd] p-4 text-sm leading-6 text-[#5d7479]">Você ainda não possui um ambiente {autonomous ? "autônomo" : "CLT"}. Crie o primeiro para começar.</div>}<Button onClick={() => setForm({ kind, name: autonomous ? "Meu ambiente Autônomo" : "Minha empresa" })} variant="outline" className="mt-5 w-full rounded-xl border-[#0c7474] text-[#0c7474]"><Plus className="mr-2 h-4 w-4" />Criar ambiente</Button></div></article>})}</section><section className="mt-10"><h3 className="text-xl font-bold">Ferramentas compartilhadas</h3><p className="mt-1 text-sm text-[#6f858a]">Recursos disponíveis para todos os seus ambientes.</p><div className="mt-4 grid gap-4 md:grid-cols-3">{shared.map(({ icon: Icon, title, text }) => <article key={title} className="flex items-center gap-4 rounded-2xl border border-[#deece9] bg-white p-5"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f6f1] text-[#0c7474]"><Icon className="h-5 w-5" /></span><div><h4 className="font-bold">{title}</h4><p className="text-xs text-[#6f858a]">{text}</p></div></article>)}</div></section></div>{form && <div className="fixed inset-0 z-50 grid place-items-center bg-[#062f35]/45 p-5"><form onSubmit={event => { event.preventDefault(); createWorkspace.mutate(form); }} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f6f1] text-[#0c7474]"><ShieldCheck className="h-5 w-5" /></span><div><h3 className="text-xl font-bold">Criar ambiente TST {form.kind === "autonomo" ? "Autônomo" : "CLT"}</h3><p className="text-xs text-[#6f858a]">Este contexto manterá empresas e PGRs separados.</p></div></div><label className="mt-6 block text-sm font-semibold">Nome do ambiente<Input value={form.name} onChange={event => setForm({ ...form, name: event.target.value })} className="mt-2 h-11 rounded-xl" autoFocus /></label><div className="mt-6 flex justify-end gap-3"><Button type="button" variant="ghost" onClick={() => setForm(null)}>Cancelar</Button><Button disabled={createWorkspace.isPending} className="rounded-xl bg-[#0c7474] text-white">{createWorkspace.isPending ? "Criando" : "Criar e abrir PGR"}</Button></div></form></div>}</DashboardLayout>;
}
