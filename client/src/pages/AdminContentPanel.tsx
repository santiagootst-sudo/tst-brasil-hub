import { useMemo, useState } from "react";
import { ArrowLeft, ExternalLink, Eye, EyeOff, FilePlus2, LibraryBig, Pencil, Save, ShoppingBag, Sparkles } from "lucide-react";
import { Link } from "wouter";
import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";

type Placement = "marketplace" | "library";
type MaterialStatus = "draft" | "published" | "hidden";
type MaterialFormat = "modelo" | "planilha" | "checklist" | "ebook" | "curso" | "documento" | "outro";
type SalePlatform = "hotmart" | "kiwify" | "externo" | "nenhuma";

type FormState = { placement: Placement; title: string; description: string; category: string; format: MaterialFormat; salePlatform: SalePlatform; price: string; referenceUrl: string; coverUrl: string; status: MaterialStatus; featured: boolean };

const initialForm: FormState = { placement: "marketplace", title: "", description: "", category: "", format: "modelo", salePlatform: "hotmart", price: "", referenceUrl: "", coverUrl: "", status: "draft", featured: false };

const statusLabel = { draft: "Rascunho", published: "Publicado", hidden: "Oculto" } as const;
const platformLabel = { hotmart: "Hotmart", kiwify: "Kiwify", externo: "Externo", nenhuma: "Sem venda" } as const;

function parsePrice(value: string) {
  const normalized = value.trim().replace(/\./g, "").replace(",", ".");
  if (!normalized) return null;
  const number = Number(normalized);
  return Number.isFinite(number) && number >= 0 ? Math.round(number * 100) : null;
}

function toForm(material: any): FormState {
  return { placement: material.placement, title: material.title, description: material.description, category: material.category, format: material.format, salePlatform: material.salePlatform, price: material.priceCents === null ? "" : (material.priceCents / 100).toFixed(2).replace(".", ","), referenceUrl: material.referenceUrl || "", coverUrl: material.coverUrl || "", status: material.status, featured: material.featured };
}

export default function AdminContentPanel() {
  const { user, loading } = useAuth();
  const isAdmin = user?.role === "admin";
  const utils = trpc.useUtils();
  const materialsQuery = trpc.content.adminList.useQuery(undefined, { enabled: isAdmin });
  const [form, setForm] = useState<FormState>(initialForm);
  const [editingId, setEditingId] = useState<number | null>(null);
  const [filter, setFilter] = useState<"all" | Placement>("all");
  const refresh = () => Promise.all([utils.content.adminList.invalidate(), utils.content.published.invalidate()]);
  const create = trpc.content.create.useMutation({ onSuccess: async () => { toast.success(form.status === "published" ? "Material publicado no portal." : "Material salvo como rascunho."); setForm(initialForm); await refresh(); }, onError: error => toast.error(error.message) });
  const update = trpc.content.update.useMutation({ onSuccess: async () => { toast.success("Material atualizado com sucesso."); setEditingId(null); setForm(initialForm); await refresh(); }, onError: error => toast.error(error.message) });
  const materials = materialsQuery.data ?? [];
  const filteredMaterials = useMemo(() => filter === "all" ? materials : materials.filter(item => item.placement === filter), [filter, materials]);
  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    const priceCents = parsePrice(form.price);
    if (form.price.trim() && priceCents === null) { toast.error("Informe um preço válido, como 49,90."); return; }
    const material = { ...form, priceCents, referenceUrl: form.referenceUrl.trim(), coverUrl: form.coverUrl.trim() };
    if (editingId) update.mutate({ id: editingId, material });
    else create.mutate(material);
  };
  const startEdit = (material: any) => { setEditingId(material.id); setForm(toForm(material)); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const hide = (material: any) => update.mutate({ id: material.id, material: { ...toForm(material), priceCents: material.priceCents, status: "hidden" } });

  if (loading) return <DashboardLayout title="Gestão de materiais"><div className="grid min-h-[50vh] place-items-center text-sm font-semibold text-[#668087]">Verificando a autorização administrativa...</div></DashboardLayout>;
  if (!isAdmin) return <DashboardLayout title="Acesso restrito"><div className="mx-auto max-w-xl rounded-3xl border border-[#f1d9d0] bg-white p-8 text-center"><EyeOff className="mx-auto h-9 w-9 text-[#bc4d28]" /><h2 className="mt-3 text-xl font-bold text-[#102b32]">Gestão exclusiva do Administrador Mestre</h2><p className="mt-2 text-sm leading-6 text-[#668087]">Apenas o proprietário do portal pode publicar materiais globais.</p></div></DashboardLayout>;

  return <DashboardLayout title="Gestão de materiais"><div className="mx-auto max-w-7xl space-y-7">
    <section className="relative overflow-hidden rounded-[2rem] bg-[#073b43] px-6 py-8 text-white shadow-[0_24px_56px_rgba(7,59,67,.17)] lg:px-9"><div className="pointer-events-none absolute -right-16 -top-20 h-60 w-60 rounded-full bg-[#7fe0c4]/20 blur-3xl" /><div className="relative flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-2xl"><Link href="/admin" className="inline-flex items-center gap-1.5 text-xs font-bold text-[#b7ead9] hover:text-white"><ArrowLeft className="h-3.5 w-3.5" /> Voltar para acessos</Link><div className="mt-5 flex items-center gap-2 text-xs font-bold uppercase tracking-[.16em] text-[#a9e8d5]"><Sparkles className="h-4 w-4" /> Central de conteúdo</div><h2 className="mt-3 font-display text-3xl font-bold tracking-tight lg:text-4xl">Publique materiais sem nova atualização do site.</h2><p className="mt-3 text-sm leading-6 text-[#c8e7df]">Cadastre ou atualize uma oferta do Marketplace e um material da Biblioteca. Após publicar, a novidade aparece automaticamente para os usuários do portal.</p></div><div className="rounded-2xl border border-white/15 bg-white/10 px-4 py-3 text-sm"><p className="font-semibold text-white">{materials.filter(item => item.status === "published").length} publicados</p><p className="mt-1 text-xs text-[#c3e4dc]">{materials.filter(item => item.placement === "marketplace").length} no Marketplace · {materials.filter(item => item.placement === "library").length} na Biblioteca</p></div></div></section>

    <section className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_minmax(340px,.75fr)]"><form onSubmit={submit} className="rounded-[1.75rem] border border-[#dcebe8] bg-white p-5 shadow-[0_14px_38px_rgba(16,43,50,.07)] lg:p-7"><div className="flex flex-col gap-3 border-b border-[#edf3f1] pb-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#0c7474]">{editingId ? "Editar material" : "Novo material"}</p><h3 className="mt-1 text-xl font-bold text-[#102b32]">{editingId ? "Atualize ou oculte a publicação" : "Monte a próxima publicação"}</h3></div>{editingId && <Button type="button" variant="outline" onClick={() => { setEditingId(null); setForm(initialForm); }} className="rounded-xl border-[#cfe4de] text-[#0c7474]">Cancelar edição</Button>}</div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-xs font-bold text-[#315158]">Destino<select value={form.placement} onChange={event => setForm(current => ({ ...current, placement: event.target.value as Placement, salePlatform: event.target.value === "library" ? "nenhuma" : current.salePlatform }))} className="mt-1.5 h-11 w-full rounded-xl border border-[#dcebe8] bg-white px-3 text-sm font-semibold text-[#102b32]"><option value="marketplace">Marketplace (venda)</option><option value="library">Biblioteca (consulta)</option></select></label><label className="text-xs font-bold text-[#315158]">Estado<select value={form.status} onChange={event => setForm(current => ({ ...current, status: event.target.value as MaterialStatus }))} className="mt-1.5 h-11 w-full rounded-xl border border-[#dcebe8] bg-white px-3 text-sm font-semibold text-[#102b32]"><option value="draft">Rascunho</option><option value="published">Publicado</option><option value="hidden">Oculto</option></select></label></div><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-xs font-bold text-[#315158]">Título<Input value={form.title} onChange={event => setForm(current => ({ ...current, title: event.target.value }))} required placeholder="Ex.: Planilha de controle de EPIs" className="mt-1.5 h-11 rounded-xl border-[#dcebe8]" /></label><label className="text-xs font-bold text-[#315158]">Categoria<Input value={form.category} onChange={event => setForm(current => ({ ...current, category: event.target.value }))} required placeholder="Ex.: EPIs e estoque" className="mt-1.5 h-11 rounded-xl border-[#dcebe8]" /></label></div><label className="mt-4 block text-xs font-bold text-[#315158]">Descrição<Textarea value={form.description} onChange={event => setForm(current => ({ ...current, description: event.target.value }))} required rows={4} placeholder="Explique o que o TST recebe ou consulta com este material." className="mt-1.5 rounded-xl border-[#dcebe8]" /></label><div className="mt-4 grid gap-4 md:grid-cols-3"><label className="text-xs font-bold text-[#315158]">Formato<select value={form.format} onChange={event => setForm(current => ({ ...current, format: event.target.value as MaterialFormat }))} className="mt-1.5 h-11 w-full rounded-xl border border-[#dcebe8] bg-white px-3 text-sm"><option value="modelo">Modelo editável</option><option value="planilha">Planilha</option><option value="checklist">Checklist</option><option value="ebook">E-book</option><option value="curso">Curso</option><option value="documento">Documento técnico</option><option value="outro">Outro</option></select></label><label className="text-xs font-bold text-[#315158]">Plataforma<select disabled={form.placement === "library"} value={form.salePlatform} onChange={event => setForm(current => ({ ...current, salePlatform: event.target.value as SalePlatform }))} className="mt-1.5 h-11 w-full rounded-xl border border-[#dcebe8] bg-white px-3 text-sm disabled:bg-[#f4f8f7]"><option value="hotmart">Hotmart</option><option value="kiwify">Kiwify</option><option value="externo">Outro site</option><option value="nenhuma">Sem venda</option></select></label><label className="text-xs font-bold text-[#315158]">Preço em R$ <span className="font-normal text-[#78928d]">(opcional)</span><Input value={form.price} onChange={event => setForm(current => ({ ...current, price: event.target.value }))} placeholder="49,90" className="mt-1.5 h-11 rounded-xl border-[#dcebe8]" /></label></div><label className="mt-4 block text-xs font-bold text-[#315158]">Link do material ou checkout<Input value={form.referenceUrl} onChange={event => setForm(current => ({ ...current, referenceUrl: event.target.value }))} type="url" placeholder={form.placement === "marketplace" ? "https://pay.hotmart.com/... ou https://pay.kiwify.com.br/..." : "https://..."} className="mt-1.5 h-11 rounded-xl border-[#dcebe8]" /></label><label className="mt-4 block text-xs font-bold text-[#315158]">Imagem de capa <span className="font-normal text-[#78928d]">(URL opcional)</span><Input value={form.coverUrl} onChange={event => setForm(current => ({ ...current, coverUrl: event.target.value }))} type="url" placeholder="https://.../capa-do-material.jpg" className="mt-1.5 h-11 rounded-xl border-[#dcebe8]" /></label><label className="mt-4 inline-flex cursor-pointer items-center gap-2 text-sm font-semibold text-[#315158]"><input type="checkbox" checked={form.featured} onChange={event => setForm(current => ({ ...current, featured: event.target.checked }))} className="h-4 w-4 rounded border-[#aacdc4] text-[#0c7474]" /> Destacar este material no catálogo</label><div className="mt-6 flex justify-end"><Button type="submit" disabled={create.isPending || update.isPending} className="h-11 rounded-xl bg-[#0c7474] px-5 font-bold text-white hover:bg-[#063b43]">{create.isPending || update.isPending ? "Salvando..." : editingId ? "Salvar alterações" : form.status === "published" ? "Publicar material" : "Salvar rascunho"}<Save className="ml-2 h-4 w-4" /></Button></div></form>

      <aside className="rounded-[1.75rem] border border-[#dcebe8] bg-[#f7fcfa] p-5 shadow-sm lg:p-6"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#e1f3ec] text-[#0c7474]"><FilePlus2 className="h-5 w-5" /></span><div><h3 className="text-base font-bold text-[#102b32]">Como publicar</h3><p className="text-xs text-[#668087]">Em quatro passos simples</p></div></div><ol className="mt-5 space-y-4 text-sm text-[#4d696c]"><li className="flex gap-3"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-[#0c7474] text-xs font-bold text-white">1</span><span>Escolha se o material aparecerá no <strong>Marketplace</strong> ou na <strong>Biblioteca</strong>.</span></li><li className="flex gap-3"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-[#0c7474] text-xs font-bold text-white">2</span><span>Informe título, categoria, explicação e formato para ajudar o profissional a identificar a solução.</span></li><li className="flex gap-3"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-[#0c7474] text-xs font-bold text-white">3</span><span>Para ofertas, cole o checkout da <strong>Hotmart</strong>, <strong>Kiwify</strong> ou outra plataforma confiável.</span></li><li className="flex gap-3"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-[#0c7474] text-xs font-bold text-white">4</span><span>Selecione <strong>Publicado</strong>. A atualização passa a aparecer automaticamente, sem novo deploy.</span></li></ol><div className="mt-6 rounded-2xl border border-[#bce0d7] bg-white p-4 text-xs leading-5 text-[#527075]"><p className="font-bold text-[#0c7474]">Importante</p><p className="mt-1">Use apenas links oficiais de checkout ou materiais que você tenha autorização para distribuir. O portal não processa pagamentos nesta etapa.</p></div></aside></section>

    <section className="rounded-[1.75rem] border border-[#dcebe8] bg-white shadow-[0_14px_38px_rgba(16,43,50,.06)]"><div className="flex flex-col gap-3 border-b border-[#edf3f1] p-5 lg:flex-row lg:items-center lg:justify-between lg:px-7"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#0c7474]">Conteúdos cadastrados</p><h3 className="mt-1 text-xl font-bold text-[#102b32]">Controle das publicações</h3></div><div className="flex flex-wrap gap-2">{(["all", "marketplace", "library"] as const).map(item => <button key={item} type="button" onClick={() => setFilter(item)} className={`rounded-xl px-3 py-2 text-xs font-bold ${filter === item ? "bg-[#0c7474] text-white" : "bg-[#edf7f3] text-[#397069]"}`}>{item === "all" ? "Todos" : item === "marketplace" ? "Marketplace" : "Biblioteca"}</button>)}</div></div><div className="divide-y divide-[#edf3f1]">{materialsQuery.isLoading ? <p className="px-6 py-12 text-center text-sm text-[#668087]">Carregando publicações...</p> : filteredMaterials.length ? filteredMaterials.map(material => <div key={material.id} className="flex flex-col gap-4 px-5 py-5 lg:flex-row lg:items-center lg:justify-between lg:px-7"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-bold text-[#102b32]">{material.title}</p><Badge className={material.status === "published" ? "border-0 bg-[#e7f7ef] text-[#18734d]" : material.status === "hidden" ? "border-0 bg-[#fff0ea] text-[#b64d2a]" : "border-0 bg-[#fff2d8] text-[#a56a14]"}>{statusLabel[material.status]}</Badge><Badge className="border-0 bg-[#e8f1ff] text-[#286099]">{material.placement === "marketplace" ? "Marketplace" : "Biblioteca"}</Badge>{material.featured && <Badge className="border-0 bg-[#fff2cc] text-[#996a0c]">Destaque</Badge>}</div><p className="mt-1 text-xs text-[#668087]">{material.category} · {platformLabel[material.salePlatform]} · atualizado em {new Date(material.updatedAt).toLocaleDateString("pt-BR")}</p><p className="mt-2 max-w-2xl text-xs leading-5 text-[#587477]">{material.description}</p></div><div className="flex shrink-0 flex-wrap gap-2"><Button type="button" size="sm" variant="outline" onClick={() => startEdit(material)} className="rounded-xl border-[#cfe4de] text-[#0c7474]"><Pencil className="mr-1.5 h-3.5 w-3.5" />Editar</Button>{material.referenceUrl && <Button asChild type="button" size="sm" variant="outline" className="rounded-xl border-[#d6e2f1] text-[#276196]"><a href={material.referenceUrl} target="_blank" rel="noreferrer"><ExternalLink className="mr-1.5 h-3.5 w-3.5" />Link</a></Button>}{material.status !== "hidden" && <Button type="button" size="sm" variant="outline" onClick={() => hide(material)} disabled={update.isPending} className="rounded-xl border-[#f0d9d1] text-[#b64d2a]"><EyeOff className="mr-1.5 h-3.5 w-3.5" />Ocultar</Button>}{material.status === "hidden" && <Button type="button" size="sm" variant="outline" onClick={() => update.mutate({ id: material.id, material: { ...toForm(material), priceCents: material.priceCents, status: "published" } })} disabled={update.isPending} className="rounded-xl border-[#b9ddd3] text-[#177054]"><Eye className="mr-1.5 h-3.5 w-3.5" />Publicar</Button>}</div></div>) : <div className="px-6 py-14 text-center"><LibraryBig className="mx-auto h-9 w-9 text-[#aac3be]" /><p className="mt-3 text-sm font-semibold text-[#315158]">Nenhum material cadastrado nesta área.</p><p className="mt-1 text-xs text-[#668087]">Use o formulário acima para preparar a primeira publicação.</p></div>}</div></section>
  </div></DashboardLayout>;
}
